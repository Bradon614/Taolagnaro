import type { DeepPartial } from "@/i18n/types";
import type { Dictionary } from "@/i18n/dictionaries/fr";

/**
 * Malagasy — scaffolded, not yet written.
 *
 * The routing, the switcher and the fallback all work; what is missing is the
 * copy. This platform exists to promote Antanosy places and businesses, and
 * shipping Malagasy that no native speaker has checked would be worse than
 * showing none: visitors would read it, and we could not vouch for a word of
 * it. A translator fills this file in and every key they add takes effect
 * immediately; anything still absent falls back to French.
 *
 * Start with the `common` and `nav` namespaces — they cover the chrome that
 * appears on every page.
 */
export const mg: DeepPartial<Dictionary> = {};
